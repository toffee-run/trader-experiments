[project]
name = "traders-experiments"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
te = "traders_experiments:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
